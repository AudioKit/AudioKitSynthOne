// The real version of this file only exists on the Link branch.  These are just stubs.
// Use care in editing this file because we do not want to merge it to Link.

class ABLLinkManager {
    static let shared = ABLLinkManager()
    var isEnabled = false
    var isConnected = false
}

class AKLinkButton: SynthButton {
    func centerPopupIn(view: UIView) {}
}
